class Consumer {
    constructor(config) {
        if (!config) {
            throw new Error('undefined config');
        }
    }

    async consume () {
        
    }
}

module.exports = Consumer;
